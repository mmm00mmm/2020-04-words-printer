import java.util.Arrays;

public class Main {
    public static void main(String[] args) {
        Factory.factory(Arrays.asList("edward gooooorey", "--compress", "--horizontal"));
        Factory.factory(Arrays.asList("edward gorey", "--capitalize", "--horizontal"));
    }
}
